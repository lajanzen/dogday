import React from "react";
import SearchMatch from "./SearchMatch";

export default {
  title: "Page/SearchMatch",
  component: SearchMatch,
  parameters: {
    layout: "fullscreen",
  },
};

export const Karl = (): JSX.Element => <SearchMatch />;
